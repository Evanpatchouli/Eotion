// On a real phone, replace 127.0.0.1 with the development machine's LAN IP.
// Production should use the deployed Eotion Web URL or a bundled local Web asset strategy.
export const EOTION_WEB_URL = 'http://127.0.0.1:5173'
